export function GET(request: Request) {
  // TODO
  // mette il proverbio del giorno (se proverbio_del_giorno uguale a 0 può esserlo se tutti sono a 1, azzera)

  // mail ad admin con proverbio del giorno
  // mail ad admin se fallisce
}